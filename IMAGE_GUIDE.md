# Baby Skin Lab 이미지 가이드 v1 — "Lab Diagram" 스타일

> 콘텐츠당 2장 · 과학 도해 일러스트 · 심플 · SVG 코드 제작 (2026-07-15 확정)

## 1. 왜 SVG 코드 제작인가 (AI 이미지 생성과 비교)

| 기준 | SVG 코드 제작 ✅ | AI 이미지 생성 (gpt-image-1) |
|---|---|---|
| 스타일 일관성 | 완벽 (같은 팔레트·선·폰트) | 매번 미묘하게 다름 |
| 한글 텍스트 | 완벽 렌더 | 한글 깨짐·오타 사고 잦음 |
| 파일 크기 | 5-15KB (Core Web Vitals 유리) | 200KB-1MB |
| 비용 | 0원 | 장당 과금 |
| 수정 | 텍스트 수정으로 즉시 | 재생성 필요 |
| 브랜드 룩 | 클리니컬 랩과 정확히 일치 | 프롬프트 복불복 |

과학 도해 + 심플 = SVG가 정답. 사진이 필요해지면(제품·인물) 그때 별도 논의.

## 2. 콘텐츠당 2장 구성 공식

| 이미지 | 역할 | 위치 | 예시 (Day 1) |
|---|---|---|---|
| **① 원리도** | 글의 핵심 개념을 한 장으로 | 첫 번째 핵심 섹션 뒤 | 로션·크림·연고 수분:유분 비율 비교 |
| **② 실전도** | 행동 가이드·타임라인·체크 | 실전 섹션 뒤 | 목욕 후 3분 보습 골든타임 타임라인 |

원리도는 "왜"를, 실전도는 "어떻게"를 그린다. 장식용 이미지 금지 — 본문 없이 이미지만 봐도 정보가 전달되어야 한다.

## 3. 스타일 규격 — 모바일 퍼스트 (2026-07-15 v2 개정)

> **왜 v2인가**: 모바일에서 figure는 약 330px 폭으로 표시된다 (viewBox 1200 기준 0.275배 축소). v1의 22px 라벨은 모바일에서 6px가 되어 읽을 수 없었다. → **텍스트를 도형의 보조로 강등하고, 폰트 하한을 강제한다.**

### 텍스트 룰 (가장 중요)
- **문장 금지. 라벨만.** 이미지당 단어 15개 이내 (숫자 제외)
- **SVG 안에 제목 넣지 않기** — 제목·설명은 figcaption의 몫. 이미지는 도형과 숫자로 말한다
- **폰트 하한 (viewBox 1200 기준)**: 라벨 36px 미만 금지 · 핵심 숫자/키워드는 60-110px로 큼직하게
- 모바일 검증 기준: 330px 폭으로 줄었을 때 모든 글자가 10px 이상이어야 함

### 도형 룰
- 요소(도형 그룹) **3-4개 이내** — 많으면 이미지를 2장으로 쪼갠다
- 도형을 화면 가득 크게, 여백 과감하게. "포스터"가 아니라 "아이콘의 확대판" 감각
- viewBox `0 0 1200 800` (3:2), 흰 배경, 테두리 없음
- 선 굵기 4-6px, 둥근 끝, 모서리 radius 14-20

### 팔레트 (5색 이내)
- 주선·강조: teal `#0ea5a4` / 진한 강조 `#0b8483`
- 보조 채움: teal pale `#e6f7f7`, amber pale `#fef3c7`
- 텍스트: ink `#0f172a`, 보조 `#64748b` / 위험 표시만 `#ef4444`

### 타이포·워터마크
- 소스 SVG는 `font-family="Pretendard, sans-serif"`로 작성. **빌드 시 `scripts/embed_font.py`가 사용된 글자만 Pretendard 서브셋을 @font-face(base64)로 임베드**하므로, img로 삽입돼도·sharp로 래스터돼도 어디서나 Pretendard로 렌더된다 (2026-07-18 도입). 소스에 직접 임베드하지 말 것 — 빌드가 처리한다.
- 워터마크: 우하단 `babyskinlab.com` 20px `#cbd5e1` (워터마크만 폰트 하한 예외)

### 금지
그라데이션 남용 · 3D · 클립아트 감성 · 이모지 · 문장형 텍스트 · SVG 내 제목/부제

## 4. 파일·SEO 규칙 (이미지 SEO 7체크)

1. 파일: `public/diagrams/{글-slug}-1.svg`, `-2.svg` (slug와 파일명 일치)
2. 본문 삽입: `<figure>` + `<img loading="lazy" width="1200" height="800">` + `<figcaption>` — 캡션은 이미지 설명이 아니라 **본문 takeaway 사실 한 줄**
3. `alt`: 도해가 전달하는 정보를 문장으로 (예: "로션 크림 연고의 수분 유분 비율 비교 — 로션 7:3, 크림 5:5, 연고 2:8")
4. frontmatter `images:` 배열 → Article 스키마 `image` 자동 출력
5. sitemap `image:image` 자동 출력 (astro.config가 public/diagrams 스캔)
6. 표시 폭: figure max-width 640px 중앙 정렬 (본문 대비 60-75%)
7. width/height 속성 명시 (CLS 방지)

## 4-1. Hero(대표) 이미지 — 검색 섬네일용 (2026-07-18 도입)

글마다 **1장**의 hero 이미지를 만든다. 구글·네이버·카카오 검색/공유 섬네일, `/guides/` 목록 카드, 본문 상단 리드 이미지로 쓰인다.

- **규격 `1200 × 630` (OG 1.91:1)** — 이 비율이라야 구글·네이버·카카오 어디서도 잘리지 않는다. PNG로 출력 (SVG는 카카오·네이버 og:image 미지원).
- 레이아웃(고정 템플릿, `scripts/build_heroes.py`): 좌측 **질문형 헤드라인**(2줄, 핵심어 teal) + 배지 pill + 우측 pale teal 패널 안 **라인 아이콘 1개**. 상단 `Baby Skin Lab` 워드마크, 하단 `babyskinlab.com`.
- 헤드라인은 검색 발화형 질문으로: 예) "아기 얼굴 오돌토돌, / 원인이 뭘까?" · "전성분표, / 어떻게 읽을까?"
- 아이콘은 축(증상=face, 보습=drop, 성분=list, 목욕=thermo, 자외선=sun 등)에 맞춰 `ICONS` 딕셔너리에서 선택하거나 신규 추가.
- og:image 태그·twitter card·Article 스키마 image·목록 섬네일은 **slug 관례(`/og/{slug}.png`)로 자동 연결** — frontmatter 없어도 됨. 필요 시 `heroImage`/`heroAlt`로 재정의.

## 5. 제작 워크플로 (매일 발행 cron에 포함)

1. 글 작성 후 원리도·실전도 주제 결정 (2절 공식)
2. **소스 SVG를 `img-src/`에 작성**:
   - 다이어그램 2장 → `img-src/{slug}-1.svg`, `img-src/{slug}-2.svg`
   - hero 1장 → `scripts/build_heroes.py`의 `HEROES` 리스트에 항목 추가 후 `python scripts/build_heroes.py`
3. **`python scripts/build_images.py` 실행** — img-src 전체를 폰트 임베드해 `public/diagrams/`(SVG)·`public/og/`(hero PNG)로 출력
4. 본문에 figure 2개 삽입 + frontmatter `images` 배열 기입 (hero는 자동 연결, 본문 삽입 불필요)
5. **모바일 검증 스크립트 실행 (필수)** — `python scripts/check_diagram.py {slug}` (img-src 소스 대상)
   - 워터마크 제외 최소 폰트 36px 이상, 모바일 환산 10px 이상
   - 단어 수 15개 이내
   - 통과 못 하면 폰트를 키우거나 텍스트를 줄여서 재작성

### 검증 기준 요약 (모바일 327px 기준, 배율 0.273)

| viewBox 폰트 | 모바일 실제 | 판정 |
|---|---|---|
| 110px (핵심 숫자) | 30.0px | 아주 잘 보임 |
| 54px (주요 라벨) | 14.7px | 잘 읽힘 |
| 42px (범례) | 11.4px | 통과 |
| 38px (보조 라벨) | 10.4px | 하한 |
| 34px | 9.3px | ❌ 미달 — 금지 |

## 6. 다이어그램 유형 레퍼토리 (반복 사용)

비교 바(비율·강도) · 타임라인(시간 흐름) · 단면도(피부 층) · 순서도(판단 분기) · 체크리스트 카드 · 온도계/게이지(수치 기준) · 지도형(부위 표시) — 글마다 유형을 달리해 단조로움 방지.
